import styled from "styled-components";

export const ImageTextureStyles = styled.div`
.image__wrapper {
    display: flex;
    z-index: -10;
    width: 100%;
    height: 150vh;
    align-items: center;
    justify-content: space-between;
    position: fixed;
    top: -20%;
    left: 0;
    user-select: none;
}
`