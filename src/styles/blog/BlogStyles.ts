import styled from 'styled-components';

export const BlogStyles = styled.div`
    p.desc {
        color: var(--text-primary);
        font-size: 14px;
        margin-inline: 15%;
        margin-block: 4rem
    }
`