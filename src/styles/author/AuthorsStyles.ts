import styled from 'styled-components';

export const AuthorsStyles = styled.div`
        margin-inline: 15%;
    p.desc {
        color: var(--text-primary);
        font-size: 14px;
        margin-block: 4rem
    }
`